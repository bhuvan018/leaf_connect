package utils

// InitStorage is a no-op since we're now using PostgreSQL
// Kept for compatibility with existing code
func InitStorage() {
        // No-op, using PostgreSQL now
        // Database initialization happens in InitDB()
}
